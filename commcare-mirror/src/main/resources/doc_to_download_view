{"map":"function(doc){
    if(( doc.base_doc === 'CouchUser' || doc.doc_type === 'Domain' || doc.doc_type == 'Group' || doc.doc_type == 'CaseRepeater' || doc.doc_type == 'Application' || doc.doc_type == 'RemoteApp') && doc.copy_of == null) {
        emit(doc._id, null);
    }
}"}
